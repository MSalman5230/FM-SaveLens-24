{
  "name": "fm24-scout",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24.14.0" },
  "scripts": {
    "start": "node server/index.ts",
    "test": "node --test tests/*.test.ts",
    "build": "npm --prefix web run build",
    "inspect": "node research/inspect.ts"
  }
}
